Graph : {
	view : {
		id, //id grafu
		width,
		height,
		bgSelectionHelper, //prostok¹t raphaela odpowiadaj¹cy za "reakcjê z t³em" selekcja, drag 'n drop
		nodeVisualiser {
			gradient { service, functionality, ... },
			getBlankNode(),
			visualiseNode(),
			extendVisualisation(),
			draw_unknownNode(),
			draw_functionalityNode(),
			draw_...Node (), //service, mediator
			
		}
		graph_view { [], [] }, //nodes, edges
		init(),
		drawGraph(),
		highlightAll(),
		removeHighlightAll()
		
		private:
		bgStart(),
		bgMove(),
		bgStop()
	},
	
	controler : {
		plugins [],
		data: //json z grafem po konwersji
		seGraph() ??
		init() ??
		load() // AJAX
		getNode(id),
		loadSSDL(),
		initPlugins(),
		convert()
		
	
		private:
		subgraphTree(),
		repository(),
		... // reszta wtyczek
		
	}
}